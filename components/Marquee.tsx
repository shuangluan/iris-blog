// Kept as a spacer / no-op in the soft-modern redesign. The nav floats over the
// gradient, so a marquee bar would fight the aesthetic. Left as a component in
// case you want to bring one back — just add content and export it.
export default function Marquee() {
  return null;
}
